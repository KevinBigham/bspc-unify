# Fable Priority Board — BSPC Launch Ecosystem

**Date:** 2026-07-05 · Companion to `FABLE_GOAT_REVIEW.md`
Scales: ROI/Risk-if-skipped = Low/Med/High/Critical · Effort = hours unless noted · Confidence = how sure the item is real and correctly scoped.

| # | Pri | Item | ROI | Risk if skipped | Effort | Conf | Owner | First files |
|---|-----|------|-----|-----------------|--------|------|-------|-------------|
| 1 | P0 | **Password recovery e2e** (family app): `redirectTo`, deep-link token consumption (3 formats + error params), recovery-mode auth-guard fix, set-new-password screen, `updateUser({password})`, tests + device checklist | Critical | Critical — M1/M7 hard gate; users locked out post-launch | 4–8h | 100% | Claude Code | `BSPC/ACTIVE/features/auth/api.ts`, `app/_layout.tsx`, `stores/auth-store.ts`, `lib/hooks/useAppInitialization.ts`, new `lib/auth/recovery-link.ts`, new `app/(auth)/set-new-password.tsx` |
| 2 | P0 | **Live-DB truth probe** (read-only script; run gated on Kevin's "go"): migration history vs files, `handle_new_user()` version, RLS enabled everywhere, buckets, orphan auth users, throwaway user | Critical | Critical — all RLS/auth guarantees currently unproven against prod | 2–3h to write | 95% | Claude Code writes; Kevin gates run | new `BSPC/ACTIVE/scripts/audit-prod-schema.ts` (per Mission M1) |
| 3 | P0 | **Push demo branches to origin** (`demo/expo-go-compat`, `demo/device-build`) — backup only, no deploy | Critical | Critical — entire working product state on one laptop | 1 min | 95% (verify remotes first) | **Kevin** (push prohibited this session) | — |
| 4 | P1 | **Credential hygiene**: rotate/disable demo accounts, delete throwaway test user, placeholder-ize creds in `BSPC/CLAUDE.md`, `docs/launch-runbook.md`, `.maestro/demo-account-smoke.yaml` | High | High — tracked live creds + open signup + unverified RLS | 1–2h code; rotation Kevin-gated | 90% | Split | the three files above + Supabase dashboard (Kevin) |
| 5 | P1 | **Schema ratification**: fold 00014/00015 into canonical SQL, fix `supabase/config.toml` `schema_paths`, run pgTAP under Docker (verify 343 bar on launch branch), metadata "7 groups"→9 | High | High — "schema is law" currently false; pgTAP bar unverified this cycle | 3–5h | 95% | Claude Code (+ Kevin ratifies) | `UNIFY/01_CANONICAL_SCHEMA.sql`, `BSPC/ACTIVE/supabase/config.toml`, `docs/app-store-metadata.md` |
| 6 | P1 | **Branch promotion**: Director Ruling naming launch lines; demo→main PRs both repos; Mission.md current-state + measured-bars rewrite; commit untracked UNIFY handoff docs (needs lock cleared — Kevin) | High | High — every workstream risks landing on the wrong base | 2–4h | 90% | Claude Code + Kevin approves | `UNIFY/Mission.md`, `UNIFY/18_DIRECTOR_ONBOARDING.md` |
| 7 | P2 | **Coach root typecheck → 0** (102 errors: legacy scripts, stale `User.uid`/`displayName`, mock typings) + dead-code gate (1 unused file, 2 unlisted deps, 4 unused types) | Med | Med — blocks calling Coach line shippable; no runtime impact today | 4–6h | 90% | Claude Code | `BSPC-Coach-App/scripts/*`, flagged test mocks, `src/components/charts/AttendanceHeatmap.tsx` |
| 8 | P2 | **Merge `proposal-b-config-hardening`** — fail-closed Functions service-role config replacing placeholder fallbacks | Med | Med-High at deploy time — fail-open config can mask prod misconfig | 1–2h (branch exists) | 85% | Claude Code | `BSPC-Coach-App/functions/src/config/supabase.ts` |
| 9 | P2 | **Release preflight greening** (the parts that exist): env matrix doc per Mission M5; Kevin-gated: hosted privacy/support URLs + real contact email post-lawyer | Med | Med — it's the launch dashboard; red hides regressions in green-able items | 1h code; rest Kevin | 95% | Split | `BSPC/ACTIVE/docs/app-store-metadata.md`, `docs/privacy-policy.md` |
| 10 | P2 | **pgTAP in CI** so the 343 bar can't silently rot (Docker service in the workflow) | Med | Med — this cycle proved the bar can be skipped without anyone noticing | 2–3h | 85% | Claude Code | `BSPC/.github/workflows/*` |
| 11 | P3 | **Invite-redemption mobile UI (M6)** — screen + `/invite/:token` deep link, reusing recovery's deep-link plumbing | High (later) | High at public launch; none for closed beta | 1–2 days | 95% | Claude Code | `BSPC/ACTIVE/features/family/` or new `features/invites/` |
| 12 | P3 | **Scheduler rehome decision memo (M3)** — pg_cron vs Edge Function vs stay-Firebase, per job; decision only | Med | Low now — jobs work; decision needed before Firebase cleanup | 2h memo | 90% | Claude Code drafts; Kevin/Director decides | `BSPC-Coach-App/functions/src/scheduled/*` |
| 13 | P3 | **Drift checks in CI**: canonical-SQL vs migrations vs app constants vs store-metadata group counts; tracked-credential scanner | Med | Med — this exact drift already happened once | 3–4h | 80% | Claude Code | new scripts in `BSPC/ACTIVE/scripts/` |
| 14 | P3 | **Admin approval → Edge Function/RPC** with transaction semantics (code comment already says so) | Med (later) | Low for closed beta (RLS covers) | 1 day | 85% | Claude Code | `BSPC/ACTIVE/features/admin/api.ts`, `supabase/functions/approve-family` |
| 15 | P3 | **Kevin dashboard/DNS items**: restore strict `handle_new_user()` after schema confirmed, malformed root SPF record cleanup, DMARC (optional), replace placeholder Site URL | Med | Med — deliverability + auth correctness odds and ends | ~1h total | 90% | **Kevin** (Claude drafts SQL/steps) | per `UNIFY/CODEX_HANDOFF_auth_email.md` §7 |

**Not on the board yet (deliberately):** Firebase remnant cleanup (M8), portal lint modernization, performance profiling, historical-doc rewrites — see review §14.

**Sequence in one line:** 1 → (2,3,4 same day) → 5 → 6 → 7/8/10 batch → 9 as Kevin's inputs arrive → 11 → 12+.
