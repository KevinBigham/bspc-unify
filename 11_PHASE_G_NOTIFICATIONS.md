# UNIFY/11 — PHASE G (NOTIFICATIONS): mini-plan + red-team

Status: **TRIPWIRE FIRED — awaiting D-G1..D-G6.** No Phase G code exists yet.
Baseline at scoping: BSPC 835 (TZ=UTC) + pgTAP 167 · Coach 1022 · Functions 118, all repos clean/synced.

## §0 Why the tripwire fired

The Phase F lesson ("audit every do-nothing default for auth-cutover coupling")
found the notification tier is MOSTLY do-nothing defaults, and the ones that
matter are unpinned:

1. **The evaluator's firing mechanism is unpinned.** `evaluateNotificationRules`
   is a Firestore trigger on `attendance/{id}` — dark since Phase C (ratified),
   dead forever at cutover (the F RF-2 class). D-F2 (client-invoke + sweeper)
   was ratified FOR MEDIA; extending it to attendance is presumed but not pinned,
   and the kick payload/sweep design is new surface.
2. **Coach push delivery has no pinned transport — and a seam.** Today's chain
   (notifications doc → `onNotificationCreated` → FCM to `coaches.fcmTokens`)
   dies with Firestore. The canonical transport (BSPC's `send-notification`
   Edge Function) **unconditionally inserts `in_app_notifications` rows for
   every job target** — any design where Coach writers both write in-app rows
   (required for the idempotency UNIQUE) and enqueue jobs double-notifies.
   And the Deno sender has zero tests (RG-5), so patching it can't meet our bar.
3. **OD-4 is explicitly open** (banked from Phase A: dailyDigest enumeration
   source), and the digest pref (`coaches.notificationPrefs.dailyDigest`,
   written default-true by AuthContext at signup, no toggle UI anywhere) has
   **no canonical home** — `notification_preferences` has only `push_enabled`.
4. **`ai_drafts_ready` routing is "G's whole"** (banked from F) — the category
   exists only as a declared type; no writer has ever existed. Wiring it is new
   behavior; G must decide, not assume.
5. **Nothing anywhere schedules the canonical pipeline.** `send-notification`
   and `cleanup-tokens` have no invoker — no cron, no caller, no pg_cron. The
   entire BSPC notification feature (including its in-app rows, which the
   sender creates) is dormant pending an invocation decision. Pure
   auth-cutover-coupled do-nothing default.
6. **Cross-phase finding (RG-6):** no migration has ever added a table to the
   `supabase_realtime` publication. Every Coach realtime subscription landed in
   B–F (12 tables) plus G's two depends on hosted publication config that
   exists nowhere in code.

## §1 Terrain

### Coach App (Firestore, migrating)
| Surface | State today |
|---|---|
| `services/notificationRules.ts` | CRUD on `notification_rules` by coachId + re-exports the **pure shared evaluation module** (autogenerated copy into functions; pure, no data access — survives G untouched) |
| `services/notifications.ts` | subscribe list (coachId, desc, limit 30) + unread count + markRead on `notifications`; `registerForPushNotifications` gets an **Expo** token but stores it in `coaches.fcmTokens` — **no call site anywhere**; FCM topic sub/unsub via `manageTopicSubscription` callable — **no call site** |
| `triggers/evaluateNotificationRules` | Firestore trigger, **dark since C**. Per-marking-coach (`coachId == markedBy`) enabled rules; 2 implemented triggers (`attendance_streak`, `missed_practice`; UI offers exactly these 2; the type declares 6); deterministic doc id `rule_{ruleId}_{swimmerId}_{evalDate}` + `set(merge)` = the idempotency that becomes the UNIQUE + upsert |
| `triggers/onNotification` | FCM send per token + invalid-token pruning. Dies with Firestore. Note: it feeds Expo-format tokens into `admin.messaging()` — even in principle these sends could never succeed; with zero registration call sites, **today's effective coach push delivery is ZERO** |
| `callable/manageTopics` | FCM topic machinery (`group_X`, `broadcast_all`); no producer ever sends to a topic. Dies with FCM. No functions-side test file |
| `scheduled/dailyDigest` | onSchedule 20:00; team-wide counts — attendance today where `!departedAt` (distinct swimmers), notes today, video_sessions in review — one notification per coach gated `notificationPrefs?.dailyDigest` |
| AuthContext | writes `notificationPrefs: { dailyDigest: true }` at signup (the only writer; AuthContext swap is cutover-banked) |

### BSPC (canonical-native)
| Surface | State today |
|---|---|
| `lib/notifications/push.ts` + `features/notifications/api.ts` | **LIVE**: Expo token → `push_tokens` upsert at app init; delete on sign-out; in-app list/unread/markRead (fetch, not realtime); push pref toggle → `notification_preferences` |
| `send-notification` Edge Function (Deno) | Complete but **dormant** (no invoker): drains `notification_jobs` pending → resolves targets (user / group→swimmers→families→profiles / all-approved) → pref-gates push (urgent overrides; reads `notification_preferences` + `profiles.push_enabled` fallback) → **in-app rows for ALL targets** → Expo push batches of 100 → sent/failed. **Zero tests** |
| `cleanup-tokens` Edge Function (Deno) | Dormant, untested: deletes 90-day-stale tokens + expired announcements |
| pgTAP 003 | proves `push_tokens` own-row wall (4 tests) |

### Canonical delta (what 00008 must land — all in 01, none in SQL yet)
- `notification_rules` (name, trigger 6-value domain, enabled, config JSONB,
  coach_id → profiles **RESTRICT**, timestamps + updated_at trigger, coach idx);
  staff FOR ALL policy.
- `in_app_notifications` + `category` (6-value domain), `data JSONB`,
  `rule_id` (FK → notification_rules **SET NULL**, deferred-style),
  `swimmer_id` (FK → swimmers SET NULL), `source_eval_date DATE`.
- **[P2-3] idempotency**: partial UNIQUE **expression** index
  `(rule_id, COALESCE(swimmer_id, zero-uuid), COALESCE(source_eval_date, '0001-01-01')) WHERE rule_id IS NOT NULL`
  — digest/announcement rows (NULL rule_id) deliberately unconstrained.
- **[P1-6]** the rule_id FK; **[P1-7]** recreate in-app UPDATE policy with
  explicit `WITH CHECK`; jobs policy `notification_jobs_admin` → `is_staff()`
  — **same principal set** (`is_staff()` IS `role IN ('coach_admin','super_admin')`), pure refactor, RC-3 one-migration.
- `idx_in_app_user (user_id, is_read)`.
- OD-1 applies: 01's enums land as TEXT CHECK.

### Identity subtlety (RG-7)
`notification_rules.coach_id` → **profiles.id** (D-B7 standard), but
`in_app_notifications.user_id` → **auth.users.id** (own-row RLS family). The
evaluator must map rule.coach_id → profiles.user_id for the recipient. Pinned
in tests or it WILL be gotten wrong.

## §2 Red-flag register

| # | Finding | Consequence |
|---|---|---|
| RG-1 | Firestore trigger mechanism death (evaluator, onNotification) | replacement invocation architecture required — D-G1/D-G2 |
| RG-2 | Coach push effectively never delivered today (no registration call site; Expo-token-in-FCM-sender mismatch) | "today's wall" for parity is **in-app only**; wiring real push is NEW capability — decide explicitly, not by drift |
| RG-3 | Sender's unconditional in-app insert vs writers' own idempotent in-app rows | double-notification seam if Coach writers also enqueue jobs — D-G2 |
| RG-4 | No invoker/scheduler exists for send-notification / cleanup-tokens anywhere | BSPC notification feature dormant end-to-end; cutover line required — D-G6 |
| RG-5 | Deno Edge Functions have zero test harness | modifying them violates the no-trusted-mocks bar; prefer designs that leave them untouched |
| RG-6 | No `supabase_realtime` publication membership in any migration (12 subscribed tables from B–F + 2 from G) | hosted realtime silently dead at cutover unless landed as code or runbook line — D-G5 |
| RG-7 | rules.coach_id (profiles.id) vs in_app.user_id (auth.users.id) | recipient mapping must be `profiles.id → profiles.user_id`, test-pinned |
| RG-8 | DIGEST DOCTRINE: digest carries team-wide counts (attendance/notes/videos) — staff-readable content | recipients must be provably staff-only; enumeration source decides this (enumerating prefs rows would reach parents) — D-G3 |
| RG-9 | The idempotency index is an **expression** index | `ON CONFLICT` must name the exact expressions + predicate; a naive 3-column conflict target does not match. pgTAP proves upsert behaviorally (no `col_is_unique` for expression indexes) |
| RG-10 | Digest pref's only writer is AuthContext (cutover-banked) | pref needs a canonical home in G or the digest has nothing to gate on at cutover — D-G3 |
| RG-11 | Digest re-run duplicates (Firestore `add()` per run; no idempotency key — NULL rule_id rows are deliberately unconstrained) | faithful = keep plain insert (scheduler fires once daily); noted, not "fixed" |
| RG-12 | `evaluateMissedPracticeGap`/streak history reads attendance as presence without status filters (Firestore had no absent rows in those paths) | Kevin's G rule applies: **D-C5 absent-exclusion (`NOT_ABSENT` or-filter) on every presence-meaning read** — evaluator history + digest count |

## §3 Already pinned — lands without new decisions

- 00008 shapes/policies/index per canonical delta above, OD-1 TEXT CHECK form,
  RC-3 one-migration for the policy recreations.
- FOLLOWUP #2: rule writers upsert `INSERT … ON CONFLICT … DO UPDATE` matching
  the deterministic-id overwrite semantics (expression conflict target per RG-9).
- Decision #7: `push_tokens` is the token home; `coaches.fcmTokens` dies;
  FCM topics have no canonical analog (group fan-out is sender-side resolution,
  already built) → `manageTopics` retires as a true subject deletion.
- Evaluator + dailyDigest move whole with their tests (session rule); the pure
  evaluation module is untouched (shared, autogenerated, no data access).
- Evaluator semantics verbatim: per-marking-coach rules, enabled-only, group
  scoping, threshold floors, no-history-no-fire gap rule, streak window math
  (limits 2 / max(10,t+2) / max(10,t+5)), title/body fallback strings.
- Digest semantics verbatim: 20:00 schedule (F precedent: scheduled functions
  stay in the functions package), distinct-swimmer count with
  `departed_at IS NULL` (RC-13: same meaning) **plus** D-C5 absent-exclusion
  (session rule), notes-today count, videos-in-review count, body string incl.
  pluralization.
- Coach notification list/unread/markRead → `in_app_notifications` own-row
  reads with the realtime parity playbook; UI untouched (data layer only).
- Trigger domain lands at canonical 6; client UI keeps offering exactly 2;
  the evaluator implements exactly 2 (unimplemented triggers behave as today:
  never fire).
- Migration scaffolding: `notification_rules` docs → rows (coachId via identity
  map; trigger/config verbatim); `notifications` docs → `in_app_notifications`
  (coachId → profiles.user_id; type→category; read→is_read; ruleId/swimmerId/
  evalDate → rule_id/swimmer_id/source_eval_date via maps). Pre-launch: expect
  zero/test-only rows. Behind the HARD STOP as always.

## §4 Decisions

**D-G1 — Evaluator invocation.**
(a) **RECOMMENDED — extend D-F2 verbatim: data-layer kick + scheduled sweep.**
`checkIn`/`batchCheckIn`/`checkOut` (the Firestore trigger fired on every
attendance write) fire-and-forget POST `evaluateAttendanceRules` with
**attendance row ids only**; the function re-derives swimmer/group/markedBy/
practice_date from PG (the processSession precedent — zero trust in client
payload), evaluates, upserts. Scheduled sweep re-evaluates attendance rows
with `updated_at > now()−10min` every 5 minutes (idempotent upsert absorbs
overlap; no schema marker needed). Same `x-process-secret` gate + env lines
banked at F.
(b) Scheduled-only evaluation (no kick): simpler, but trades today's
near-instant firing for up-to-N-minute latency.
(c) PG trigger evaluating in SQL: pgTAP-provable, but forks the shared
evaluation module into the database and rewrites ratified business logic —
the A3/D-D5 precedent covers atomicity seams, not rule engines.

**D-G2 — Coach push transport (and the RG-3 seam).**
(a) **RECOMMENDED — G ships coach notifications in-app only; no push wiring.**
Writers (evaluator/digest) write `in_app_notifications` directly (service
role, upsert); **no `notification_jobs` writes; the Deno sender is not
touched**. `registerForPushNotifications` re-points its storage to
`push_tokens` (canonical parity, tests move) but remains uncalled — exactly
today's effective behavior (RG-2: zero pushes delivered today). The seam
disappears instead of being patched; zero untestable surface. Coach push
wiring becomes an explicit post-cutover product line (sketch: jobs +
`skip_in_app` flag or a jest-tested Expo transport on the functions side).
(b) Wire push now via jobs + patch the sender to skip in-app inserts for
flagged jobs: one pipeline, but modifies untested Deno (RG-5) — can't meet
the bar, permanent flagged risk.
(c) Send Expo pushes directly from the functions-side writers now: jest-tested
logic, but the transport call is mock-only (the Vertex class of risk) and
"push on first insert only" needs careful upsert discipline. Viable if you
want coach push to exist before cutover; honest cost is one more mock-only
external call.

**D-G3 — OD-4: digest enumeration + pref home.**
(a) **RECOMMENDED — enumerate `profiles` WHERE staff role set (the is_staff
set), gated by a new `notification_preferences.digest_enabled BOOLEAN NOT NULL
DEFAULT TRUE` (00008).** Faithful to today (every coach signup writes
default-true; no UI changes it), gives the pref a canonical home before the
AuthContext writer retires at cutover (the column default replaces the signup
write), and satisfies RG-8 provably: recipients come from the staff role set
by construction, never from prefs rows. **Edge-case flip to ratify:** today a
coach with a MISSING pref map is skipped; with a default-true column an absent
row means enabled. The old "skips coaches without dailyDigest preference" pin
is deleted by name (its subject — the Firestore prefs map — is deleted) and
replaced by an absent-row-means-enabled pin.
(b) No pref; digest to all staff unconditionally: loses the stored bit and the
future toggle's home.
(c) JSONB prefs blob: over-general, no canonical precedent.

**D-G4 — `ai_drafts_ready` routing (banked: "G's whole").**
(a) **RECOMMENDED — re-bank as a named post-launch product line.** No writer
has ever existed; migration phases reproduce today's behavior ("migrate
faithfully, do not editorialize"). G lands the category in the CHECK domain,
so wiring later is a one-line writer + tests.
(b) Wire it now: `processAudio/VideoSession` insert an in_app row (category
`ai_drafts_ready`, recipient = session coach's user_id) when drafts reach
review. ~15 lines + 2 jest pins, but it is genuinely NEW behavior landing
mid-migration.

**D-G5 — Realtime publication (RG-6, cross-phase).**
(a) **RECOMMENDED — land it as code in 00008:** idempotent DO-block
`ALTER PUBLICATION supabase_realtime ADD TABLE …` for all 14 subscribed
tables (swimmers, attendance, swim_results, goals, group_notes, swimmer_notes,
swimmer_voice_notes, swimmer_coach_profile, audio_sessions,
audio_session_drafts, video_sessions, video_session_drafts + G's
notification_rules, in_app_notifications), guarded against already-present
membership; pgTAP proves `pg_publication_tables` membership. Realtime
postgres_changes respects RLS, so publication membership widens nothing.
(b) Runbook/cutover line only (dashboard config): leaves the gap latent and
unprovable.

**D-G6 — Scheduling the dormant canonical pipeline (sender + cleanup-tokens).**
(a) **RECOMMENDED — G changes no Deno code; the cutover runbook gains a line:**
"schedule `send-notification` + `cleanup-tokens` (Supabase cron) at cutover
staging; verify one job drains end-to-end (in-app rows + push) on the
throwaway project before flipping". Status quo is dormant-by-design pre-launch.
(b) pg_cron + pg_net in a migration: code-first but pulls the service key into
DB settings and adds extension surface — poor trade pre-launch.

## §5 Proposed commit sequence (after ratification — one green commit each)

1. **NOTES ratification log** (UNIFY).
2. **BSPC `00008_phase_g_notifications.sql` + pgTAP 011.** The §1 canonical
   delta + `digest_enabled` (D-G3) + publication adds (D-G5). pgTAP: shape ×
   (columns/PK/FKs/trigger/indexes incl. expression-index existence); CHECK
   domains (trigger 6, category 6); FK behaviors (RESTRICT, SET NULL both
   pointers); **walls** — rules staff-only (staff CRUD ✓; family/guardian/
   pending/anon read 0, write 42501), in_app own-row (can't read/update
   another's; **[P1-7] proof: UPDATE reassigning user_id away fails**), jobs
   staff wall same-set; **idempotency behavioral proofs** (upsert same triple
   → 1 row updated not duplicated; NULL swimmer/eval dedup via sentinels;
   NULL-rule_id rows insert freely ×2); publication membership ×14;
   digest_enabled default TRUE.
3. **Coach `notificationRules.ts` swap + tests** — CRUD → `notification_rules`
   (coach_id profiles.id per D-B7), realtime-parity subscribe; pure evaluation
   helpers re-exported unchanged (their 19 tests untouched).
4. **Coach `notifications.ts` swap + tests** — list/unread/markRead →
   `in_app_notifications` (own-row; realtime parity); register/unregister →
   `push_tokens` upsert/delete (still uncalled, storage parity);
   **manageTopics client functions deleted** (subject deletion: FCM topics) —
   topic tests deleted by name; AuthContext sign-out topic call removed.
5. **Functions: evaluator rebuild + retirements.** New HTTPS
   `evaluateAttendanceRules` (secret gate; id-payload; PG re-derive; D-C5
   filters; upsert with expression conflict target; recipient =
   rule-owner profiles.user_id) + sweep schedule per D-G1; attendance.ts
   data-layer kick (exactly-one-kick pins); **DELETE
   `evaluateNotificationRules` trigger, `onNotification`, `manageTopics`** —
   evaluator tests re-pointed (subjects kept), onNotification's 6 tests
   deleted by name (subject: FCM transport + fcmTokens pruning, dies with
   D-G2(a); replacement proofs: writers' in_app pins + pgTAP walls).
6. **Functions: dailyDigest rewrite + tests** — PG reads (D-C5 +
   `departed_at IS NULL` count, notes-today, videos-in-review), enumeration +
   gate per D-G3, per-coach in_app inserts (NULL rule_id), body verbatim;
   missing-pref pin replaced per D-G3(a).
7. **`migration/notifications/README.md`** scaffolding + **NOTES landed log**;
   push all repos; report.

Counts impact (estimate): pgTAP +35–45; Coach client net ≈ −4+new pins
(topic deletions vs realtime/own-row pins); Functions −6 (onNotification)
−4+4 (digest re-point) +new evaluator/sweep/kick suites. BSPC jest 835
untouched (BSPC app code unchanged; its notification reads already canonical).
BSPC tsc stays clean; Coach bar stays jest-only.

## §6 Red-team (attacking my own recommendations)

- **D-G2(a) means coaches still get zero pushes after G.** That IS today's
  effective behavior, but if you think of push as part of the feature's
  promise, (c) is the honest alternative — its cost is one more mock-only
  external transport (the accepted Vertex class), not architecture.
- **Kick-from-checkOut**: re-evaluating on checkout can fire a rule created
  after check-in — identical to today's onDocumentWritten behavior. Faithful.
- **batchCheckIn** sends ONE kick with N ids, not N kicks; the sweep catches
  dropped kicks (at-least-once), upsert makes repeats invisible.
- **Expression-index ON CONFLICT (RG-9)** is the likeliest silent-failure
  point; jest mocks cannot prove PG semantics, so pgTAP 011 carries the
  upsert proof (DO-block double-call → single row, updated fields).
- **The digest count changes meaning by exactly one ratified step**: it adopts
  D-C5 absent-exclusion (your standing G rule). An absent-marked row (possible
  since C; impossible in Coach Firestore) no longer counts as "attended".
  Pinned in the new tests; old pin's subject (Firestore docs) is deleted.
- **Missing-pref flip (D-G3(a))** is a real behavior change at one edge —
  named deletion + new pin, ratify it explicitly.
- **Publication DO-block** must tolerate tables already in the publication
  (local stack may pre-add some) — membership-checked ADDs, idempotent under
  `db reset`.
- **If notification_rules realtime needs RLS context**, postgres_changes
  enforces RLS per-subscriber — staff-only rules emit only to staff. No
  widening through the publication.
- **Digest enumerated from profiles role set**: a future role widening would
  widen digest recipients silently — the pgTAP wall on rules/in_app doesn't
  guard enumeration (it's service-role). Jest pins the role filter string.

## §7 [DECIDE] — for Kevin

- **D-G1**: evaluator invocation — (a) kick from attendance data layer +
  5-min sweep (D-F2 extended, recommended) / (b) scheduled-only / (c) SQL.
- **D-G2**: coach push — (a) in-app only this phase, sender untouched,
  push = post-cutover product line (recommended) / (b) patch Deno sender /
  (c) jest-tested functions-side Expo transport now (mock-only send).
- **D-G3**: digest — (a) enumerate staff roles + `digest_enabled` column
  default TRUE, absent-row⇒enabled flip ratified (recommended) / (b) no pref /
  (c) JSONB.
- **D-G4**: `ai_drafts_ready` — (a) re-bank as product line, land category
  domain only (recommended) / (b) wire in the media pipeline now.
- **D-G5**: realtime publication — (a) code in 00008 + pgTAP ×14
  (recommended) / (b) runbook line only.
- **D-G6**: pipeline scheduling — (a) no Deno changes; cutover-runbook
  cron + end-to-end drain verification line (recommended) / (b) pg_cron.
